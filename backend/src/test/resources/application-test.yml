spring:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  cache:
    type: none
  data:
    redis:
      host: localhost
      port: 6379

jwt:
  secret: test-secret-key-minimum-32-chars-long-for-tests
  expiration: 86400000
